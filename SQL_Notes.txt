DATABASE --->store data
          Manipulation  , insertion,delete,update,search 
DBMS - Database management system
      -- data stores on table 
        --- multiple tables , database 
RDBMS -- Relational database management system --- table related 

Tools --> Mysql , SQLServer , MS-access ,oracle 

MYSQL 

SQL --> structure query language

SQL -- database --Table/Object --data --query 

Types query 
1. DDL      Data definition Language    --- create ,alter ,drop (related to table)
2. DML      Data Manipulation Langauge --- insert ,update ,delete  (related to data)
3. DQL      Data Query Langauge      ---select 
4. DCL      Data control Langaufe --- grant/revoke 

Student_table 
roll,name,marks --table fieled 
101,'Bhavya,120  --- data
102 ,Disha ,200 ---data

1. To create databse 

--- > create database db1
---> use db1

Create table 

create table <<table_name>> (field_name datatype(size) ,field_name datatype(size)  , field_name datatype(size) ,
field_name datatype(size) ....)

data types -- numeric(int,decimal) , string ,datetime ,object 

create table student(roll_no int,s_name varchar(20) ,age int)

insert into <<table_name>> values(101,'Disha','disha@gmail.com')

constraints  ---
1. Primary key  --- To uniquely indentify record,not dublicate value  e.g roll_no ,emp_id 
2. Not null --- 
3. foreighn key --- references to another table 
4. default -- set default value to field 


create table student(roll_no int primary key,s_name varchar(20) not null ,age int);

8th Jan

create table employee 
(eid int primary key,
ename varchar(20),
email varchar(20) not null,
city varchar(10) default "ahmedabad"
);

select * from employee;

insert into employee values(1,"Paresh","Paresh@gmail.com","Baroda");
insert into employee(eid,ename,email) 
values(11,"Ramesh","ramesh@gmail.com");


select * from employee;

insert into employee values(2,"Paresh","pa@gmail.com","Surat"),
(3,"qwert","q@gmail.com","Surat"),
(4,"sasa","G@gmail.com","Bhavnagar"),
(5,"cccc","c@gmail.com","Baroda");

select * from employee;

--forighn key 


student 
bhavya - surname - adress -- c_no--fees details--education details --- SE  -- 10:11_30 : facultyname , address,technology
Disha -surname -aaddress -c_no - ffes details --education --se -10:1130 :facult....


Master 
Student_Master
sid , sname , address , gender ,education ....

Faculty_master
fid , fname ,address ,gender ,eduication ....

Batch_Master
batch_id  timing duration technology
101         10-11:30 2M          SE           

batch_details
batch_detail_id   sid   fid  batch_id 
101                101  101    101
102                201  101    101
103                301  101    101          

Normalization   --dublicate removes 

Employee 
eid(PK)          name          j_date          email          salary          education  did(FK)

department 
did(PK)          dname          description
101          Finanace          fskdfjhsfkjshf
102          Production          .,mxvxc.v,

 * from employee;


create table Department(did int primary key,
dname varchar(20),
description varchar(200)
);

create table employee1(eid int primary key,
ename varchar(20),
email varchar(20),
salary int,
did int,
foreign key(did) references Department(did)
);

insert into Department values(1,"Production","Department Production"),
(2,"Finance","Finance Deparement"),
(3,"Software","Software Department"),
(4,"HR","HR Department");

select * from Department;

insert into employee1 values(101,"emp1","emp1@gmail.com",23000,1),
(102,"asdemp1","emp1@gmail.com",29000,2),
(103,"qwwemp1","emp1@gmail.com",20000,2),
(104,"sdsemp1","emp1@gmail.com",21000,11);

select * from employee1;


10th Jan

Primary key    - To uniquely identify a record dept_id,emp_id
Forighn key    - To reference to another table Employee table dept_id is my FK


Course table           -- cid(PK) ,cname , duration_in_month , fees
Student table          -- sid(PK) ,sname , address , bdate , email
Enrollment table       -- enrol_id(PK) , sid(FK) ,cid(FK) ,date_enrollment

comments are -- and /*  */
DML- insert , update ,delete
DQL - select - won't affect data

select , and-or,like,order by , in, where ,between and 

select * from tablename;

--select col1,col2,col3 from tablename;

13th Jan 

--display student data
  select * from student
--- fetch/display student name and address from student table  
          select sname,address from student;
--fetch courname and fees from course table 
          select course_name,fees from course

--fetch enrolment id, student id and enrollment date from entrollment table

--fetch course details whose fees is more then 20000
- select * from course where fees>20000

fetch course details whose fees is more then 20000 and duration is more then 5 months 
select * from course where fees>20000 and duration_in_month>5;

fetch student details who are living in Nikol
-select * from student where address="Nikol"

fetch courses whose duration is in 2 to 5 month 
select * from course where duration_in_month between 2 and 5

fetch student details who leaves in nikol and c g road  --in
select * from student where address in("nikol","C G Road")

where , between and , in , and ,or 

fetch course details whose fees is more then 20000 or duration is more then 5 months 
select * from course where fees>20000 or duration_in_month>5

fetch student details  student name , alphabetically 
select * from student
select * from student order by sname


select * from student order by sname desc

fetch student details whose name start with s
like 
% , _
select * from student where sname like 's%'

fetch student details whose name ends with a
select * from student where sname like '%a';

fetch student details whose name contains i;
select * from student where sname like '%i%';

fetch student name whose name's second letter h;
select * from student where sname like '_h%';

fetch student name whose name's last second letter h;
select * from student where sname like '%h_';

fetch student name whose name contains 5 letters 
select * from student where sname like '_____';


select * from enrollment;


joining 
student name ---student 
entrolmment details --- enrolment 
sid ---join 

select eid,sname,course_id,enrol_date from enrollment,student
where student.sid=enrollment.student_id;


17th Jan
    database create
    tables create /alter/drop  DDL
    data enter / insert /delete/update  DML
    Constraints - PK,FK,Not NULL 
    queries select  where between and,and ,or, in ,like ,order by ,desc 


select * from employees where year(hire_date)=2023

select emp_name,year(hire_date) as 'Joining year',month(hire_date) from employees

select * from employees where month(hire_date)=9

select * from employees where dept_id in (2,4)

select * from employees where dept_id=2 or dept_id=4

select count(dept_id) as 'No of Department' from departments

Labtask :

CREATE TABLE customers (
    cust_id INT PRIMARY KEY,
    cust_name VARCHAR(50),
    city VARCHAR(50),
    email VARCHAR(100),
    join_date DATE
);

CREATE TABLE products (
    prod_id INT PRIMARY KEY,
    prod_name VARCHAR(50),
    category VARCHAR(30),
    price DECIMAL(10,2),
    stock INT
);

CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    cust_id INT,
    order_date DATE,
    status VARCHAR(20),
    FOREIGN KEY (cust_id) REFERENCES customers(cust_id)
);

CREATE TABLE order_items (
    order_item_id INT PRIMARY KEY,
    order_id INT,
    prod_id INT,
    quantity INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id),
    FOREIGN KEY (prod_id) REFERENCES products(prod_id)
);

enter sample data in all tables 

20th Jan
  when we have fetch data from more then 1 table then we have to joining

Display employee name, department name, and salary for all employees.
  employees , department joining on bases of common field

when there is query like dept wise , each dept  group by clause , aggregate funtion (avg,min,max,sum,count)

Lab Task :

Queries 

Display product names with their stock quantity.
Show customers whose name starts with “S”.
Display the cheapest product.
Show each customer’s name and the total value of their delivered orders.
Show the number of “Delivered” vs “Cancelled” orders.
Find customers who have never placed an order.
List all products that have been ordered more than 2 times.

22nd Jan 

  if we put condition in grouping then we have to use 'having'


--- views   --? 
tables ---
views  --- virtual table 
store procedure
functions
trigger

employee infor with department name ---each join


TRIGGER --- ? database object
              they called automatically / 
              2 -- Before and After 
                    Insert , update , delete 
                    Before delete / after delete 
                    Before insert / after insert
                    Before update / after update 
          

Function : inbuild / user defined 
          --- now , avg , sum , sum , 
                    date
                    string
                    numeric


CREATE FUNCTION GetFullName(first_name VARCHAR(50), last_name VARCHAR(50))
RETURNS VARCHAR(100)
---function name : GetFullName
   paramter first_name , lst_name
   return String 
call

GetFullName('dharmishtha','kothari') 
dharmishtha kothari 

select getFullName(firstname,lastname) from employees
select getfullname(ename,email) from employees

27th Jan
          - functions - inbuild 
                        user defined  -suyntax 
                        de_id pass total_salary

29th Jan

Procedure --- GEt order count by customer name
          --Get data By City

select * from employees 
resultset --10 records

row by row ---ro1,row2,row3,row4,....

3rd Feb

Second Example 
Tables:
customers – cust_id , cust_name , city , email , join_date 
products –  prod_id,  prod_name ,category , price , stock
orders – order_id , cust_id , order_date , status (status like delivered,pending,cancelled)
order_items – order_item_id ,order_id , prod_id ,  quantity 

Queries 

Display product names with their stock quantity.
Show customers whose name starts with “S”.
Display the cheapest product.
Show each customer’s name and the total value of their delivered orders.
Show the number of “Delivered” vs “Cancelled” orders.
Find customers who have never placed an order.
List all products that have been ordered more than 2 times.

Lab Task : 

Count how many orders each customer has placed.
Find the total quantity of products ordered in each order.
Show each customer’s name and the total value of their delivered orders.
Display the most expensive product in each category.
Find customers who have never placed an order.
Show total sales (price × quantity) of each product.
List all products that have been ordered more than 2 times.
Find the total revenue generated in 2024.
Display all orders along with customer names and order status.
Show the number of “Delivered” vs “Cancelled” orders.

Find the top 3 customers with the highest total spending.
Display the product categories ranked by total sales.
Find customers who have purchased both “Laptop” and “Headphones”.
Show products that were never ordered.
Find orders with multiple products from different categories.
Calculate each month’s total revenue and show a running total.
Display the average order value per customer.
Show the most frequently ordered product.
List customers who placed orders in at least 3 different months.
Find products that are out of stock or nearly out of stock (less than 10 units).



delimiter $$
create procedure check_Emp(id int)
begin
  declare emp_salary int;
  declare ename varchar(20);
  select salary,name into emp_salary,ename from employee where empId=id;
 
  if emp_salary>5000 then 
     select ename,"Senior Employee " as status;
  else
     select ename,"Normal Employee" as status;
  end if;
  
end$$
call check_Emp(3);

lab Task : add more condition in above procedure 
between and 

delimiter $$
create procedure add_emp()
begin
    declare i int default 1;
    while i<=10 do
      insert into Employee values(109+i,'test','sales',2000);
      set i=i+1;
    end while;
end$$

call add_emp();


---5th Feb

Rollback 
commit


Trigger ---even occurece task  , automatic execute eg. record delete backup , logges 
insert before/after
delete before/after
update before/after

create table emp_copy(id int,create_at timestamp);

delimiter //
create trigger trg_insert_emp 
after INSERT
on employee 
for each row
begin
    insert into emp_copy values(NEW.empId,now());
end //

INSERT INTO EMPLOYEE VALUES (0001, 'Clark', 'Sales');
INSERT INTO EMPLOYEE VALUES (0002, 'Dave', 'Accounting');
INSERT INTO EMPLOYEE VALUES (6666, 'Ava', 'Sales');

SELECT * from emp_copy;

select * from employee;



